// تطبيق React Native / Expo جاهز للتطوير والنشر على Google Play Store
// اسم التطبيق: قطرة حياة - ولاية الشلف
// التقنيات المقترحة:
// - Expo React Native
// - Firebase Authentication
// - Firestore Database
// - OneSignal Notifications
// - Google Maps API
// - Android Studio لبناء ملف APK/AAB

export default function BloodDonationChlefApp() {
  const donors = [
    { name: 'أحمد بن عمر', group: 'O+', city: 'الشلف', status: 'متاح الآن' },
    { name: 'محمد زروقي', group: 'A+', city: 'تنس', status: 'متاح' },
    { name: 'إيمان بوزيد', group: 'B-', city: 'وادي الفضة', status: 'قريب' },
  ];

  const requests = [
    { hospital: 'مستشفى الشلف', blood: 'AB+', urgent: 'عاجل' },
    { hospital: 'مستشفى تنس', blood: 'O-', urgent: 'خلال اليوم' },
  ];

  return (
    <div className="min-h-screen bg-red-50 p-4 text-right" dir="rtl">
      <div className="max-w-6xl mx-auto">
        {/* Header */}
        <div className="bg-gradient-to-l from-red-700 to-red-500 text-white rounded-3xl p-6 shadow-xl mb-6">
          <div className="flex flex-col md:flex-row justify-between items-center gap-4">
            <div>
              <h1 className="text-4xl font-bold mb-2">قطرة حياة - ولاية الشلف</h1>
              <p className="text-lg opacity-90">
                تطبيق احترافي للتبرع بالدم وربط المتبرعين بالمحتاجين بسرعة.
              </p>
            </div>
            <div className="bg-white text-red-600 rounded-2xl px-6 py-4 shadow-lg">
              <p className="text-sm">عدد المتبرعين النشطين</p>
              <h2 className="text-3xl font-bold">1,284</h2>
            </div>
          </div>
        </div>

        {/* Quick Actions */}
        <div className="grid md:grid-cols-3 gap-4 mb-8">
          <button className="bg-red-600 hover:bg-red-700 text-white rounded-2xl p-6 shadow-lg transition-all">
            <h2 className="text-2xl font-bold mb-2">تبرع الآن</h2>
            <p>سجل نفسك كمتبرع بالدم</p>
          </button>

          <button className="bg-white hover:shadow-xl rounded-2xl p-6 border border-red-200 transition-all">
            <h2 className="text-2xl font-bold text-red-600 mb-2">طلب دم</h2>
            <p className="text-gray-600">أنشئ طلب عاجل للمستشفيات</p>
          </button>

          <button className="bg-white hover:shadow-xl rounded-2xl p-6 border border-red-200 transition-all">
            <h2 className="text-2xl font-bold text-red-600 mb-2">أقرب مركز</h2>
            <p className="text-gray-600">ابحث عن مراكز التبرع القريبة</p>
          </button>
        </div>

        {/* Emergency Requests */}
        <div className="bg-white rounded-3xl p-6 shadow-lg mb-8">
          <div className="flex items-center justify-between mb-4">
            <h2 className="text-2xl font-bold text-red-700">طلبات عاجلة</h2>
            <span className="bg-red-100 text-red-700 px-4 py-2 rounded-full text-sm">
              مباشر
            </span>
          </div>

          <div className="grid md:grid-cols-2 gap-4">
            {requests.map((req, index) => (
              <div
                key={index}
                className="border border-red-200 rounded-2xl p-5 hover:shadow-md transition-all"
              >
                <div className="flex justify-between items-center mb-3">
                  <h3 className="text-xl font-bold">{req.hospital}</h3>
                  <span className="bg-red-600 text-white px-3 py-1 rounded-full text-sm">
                    {req.urgent}
                  </span>
                </div>
                <p className="text-gray-700 mb-4">
                  فصيلة الدم المطلوبة: <span className="font-bold text-red-600">{req.blood}</span>
                </p>
                <button className="w-full bg-red-600 hover:bg-red-700 text-white py-3 rounded-xl font-semibold">
                  تبرع لهذا الطلب
                </button>
              </div>
            ))}
          </div>
        </div>

        {/* Donors */}
        <div className="bg-white rounded-3xl p-6 shadow-lg mb-8">
          <h2 className="text-2xl font-bold text-red-700 mb-5">متبرعون متاحون</h2>

          <div className="grid md:grid-cols-3 gap-4">
            {donors.map((donor, index) => (
              <div
                key={index}
                className="bg-red-50 border border-red-100 rounded-2xl p-5"
              >
                <div className="flex items-center justify-between mb-3">
                  <h3 className="font-bold text-lg">{donor.name}</h3>
                  <div className="bg-red-600 text-white w-12 h-12 rounded-full flex items-center justify-center text-lg font-bold">
                    {donor.group}
                  </div>
                </div>

                <p className="text-gray-600 mb-2">📍 {donor.city}</p>
                <p className="text-green-600 font-semibold mb-4">● {donor.status}</p>

                <button className="w-full border border-red-600 text-red-600 hover:bg-red-600 hover:text-white py-2 rounded-xl transition-all">
                  تواصل الآن
                </button>
              </div>
            ))}
          </div>
        </div>

        {/* Features */}
        <div className="grid md:grid-cols-4 gap-4 mb-8">
          {[
            'إشعارات فورية',
            'تحديد الموقع',
            'دردشة مباشرة',
            'إحصائيات التبرع',
          ].map((feature, index) => (
            <div
              key={index}
              className="bg-white rounded-2xl p-5 text-center shadow-md"
            >
              <div className="text-4xl mb-3">🩸</div>
              <h3 className="font-bold text-lg text-red-700">{feature}</h3>
            </div>
          ))}
        </div>

        {/* Footer */}
        <div className="bg-red-700 text-white rounded-3xl p-6 text-center">
          <h2 className="text-2xl font-bold mb-2">ساهم في إنقاذ الأرواح</h2>
          <p className="opacity-90 mb-4">
            تطبيق خاص بولاية الشلف لتسهيل الوصول إلى المتبرعين بالدم.
          </p>

          <div className="flex flex-wrap justify-center gap-4 mt-4">
            <button className="bg-white text-red-700 px-6 py-3 rounded-xl font-bold hover:scale-105 transition-all">
              تحميل التطبيق
            </button>
            <button className="border border-white px-6 py-3 rounded-xl font-bold hover:bg-white hover:text-red-700 transition-all">
              انضم كمتبرع
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}
